fastapi==0.115.8
uvicorn[standard]==0.34.0
python-multipart==0.0.20

numpy==2.1.3
opencv-python-headless==4.10.0.84

# Face detection (Faceplugin OSS) uses PyTorch.
# We install torch separately via the CPU-only wheel index in scripts/bootstrap.sh.
